# Python dependencies for the GeoAI Talent Agent data-ingestion script (Phase 1),
# i.e. db/seed/ingest.py. NOT needed to run the app: the web app is Next.js/Node
# and its dependencies live in package.json (see Dockerfile / DOCKER.md).
# The FastAPI gateway has its own list in fastapi-gateway/requirements.txt.
#
# Set up:   python -m venv .venv
# Windows:  .venv\Scripts\activate
# macOS:    source .venv/bin/activate
# Install:  pip install -r requirements.txt
#
# Pinned to the versions this project was verified against.

openpyxl==3.1.5
python-dotenv==1.2.2

# Loading into Postgres/Supabase — choose one:
supabase==2.31.0          # supabase-py client
# psycopg[binary]>=3.2    # direct PostgreSQL connection (uncomment if loading via SQL instead)
